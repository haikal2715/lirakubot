name: lirakubot
type: buildpack

build:
  builder: heroku/buildpacks:20

run:
  command: python3 main.py

ports:
  - port: 8000
    protocol: http

envs:
  - key: BOT_TOKEN
    value: ${BOT_TOKEN}
  - key: EXCHANGE_API_KEY
    value: ${EXCHANGE_API_KEY}
